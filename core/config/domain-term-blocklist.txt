# Domain-term blocklist for core file lint (case-sensitive grep).
# One term per line. Lines starting with # are ignored.
# Add new domain terms as they appear in your project.

# Cloud provider terms (both cases where needed)
security group
Security group
security groups
Security groups
Lambda
DynamoDB
CloudWatch
EFS
boto3
API Gateway

# Project-specific terms
NPC
Roblox
AyoBridge
Studio Bridge
AYOAI_API_KEY
AYO_OPERATOR_KEY
game session
Game session

# External-path directory naming variants (g-115-432, 2026-05-08).
# Catches Ayoai-* in core docstrings/rules where they leak. Test-fixture
# files (test-*.sh/.py) and files marked domain-leak-exempt are excluded
# automatically by domain-leak-check.sh. orphan-root-sweep.sh uses these
# strings as detection data and carries the exempt marker.
Ayoai-World
Ayoai-Meta
Ayoai-Mind
Ayoai-Alpha
Ayoai-Bravo
My-Meta

# Domain-specific identifiers surfaced during 2026-05-18 packaging
# audit (plan Phase 4.4). Each is a real AyoAI-deployment identifier
# that should not appear in framework files. Add new terms here as
# the audit catches them.
jose
IAUS
richmondkey
bitnet
seedgettervert
BTEvents
Roblox-Integration
Environment-Processor

# Peer-world deployment identifiers (g-115-10049 outcome 4, 2026-09-20).
# class-B deployment identities from core/config/environments/*.yaml, the
# registry domain-term-classes.md calls authoritative. Lowercase ONLY: the
# blocklist is case-sensitive and the Title-Case variants match ZERO files
# (Zds-Mind = 0), so lowercase entries are both safe and sufficient.
# Each term was positive-controlled under the REAL matcher before being added
# (grep -rnw, case-sensitive, same --include set as domain-leak-check.sh:139)
# because a term matching nothing reads identically to a clean one (guard-6233):
#   zds-mind 189 hits/35 files · claude-mind 75/32 · coach-mind 26/11 · serene-mind 11/4
# DELIBERATELY EXCLUDED, by measurement, NOT by omission:
#   `local`       — environment_id in environments/local.yaml, 685 unmarked files;
#                   collides with ordinary English and with local-path prose.
#   `ayoai-mind`  — THIS deployment's own directory name, so it rides in every
#                   absolute path on the box. The Title-Case `Ayoai-Mind` above
#                   is the correct, non-colliding form and is already listed.
zds-mind
claude-mind
coach-mind
serene-mind
